{
  "name": "agent-skills-toolkit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "demo": "node tools/validate-skills.mjs && node tools/sync-skills.mjs --dry-run --target codex",
    "test": "node --test tests/**/*.test.mjs",
    "check": "pnpm test && pnpm demo"
  }
}
